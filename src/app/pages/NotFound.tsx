import { NotFoundBlock } from 'common/components/NotFoundBlock'
import { FC } from 'react'

type Props = {}

export const NotFound: FC = (props: Props) => {
	return (
		<>
			<NotFoundBlock />
		</>
	)
}
